{
    "name": "cinema-app",
    "scripts": {
        "start": "cd server node ./bin/www && npm install",
        "heroku-postbuild": "cd client && npm install && npm run build"
    },
    "engines": {
        "node": "15.x",
        "npm": "6.x"
      }
}