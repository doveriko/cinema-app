{
    "name": "cinema-app",
    "scripts": {
        "start": "node ./bin/www",
        "heroku-postbuild": "cd client && npm install && npm run build"
    },
    "engines": {
        "node": "15.x",
        "npm": "6.x"
      }
}