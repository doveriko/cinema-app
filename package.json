{
    "name": "cinema-app",
    "scripts": {
        "start": "concurrently \"cd server node ./bin/www"\" \"cd client && npm start\"",
        "heroku-postbuild": "cd client && npm install && npm run build"
    },
    "engines": {
        "node": "15.1.0",
        "npm": "6.14.9"
    }
}
