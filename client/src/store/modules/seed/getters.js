export default {
    seedSessions(state) {
        return state.sessions;
    },
    seedMovies(state) {
        return state.movies;
    },
    seedSeats(state) {
        return state.seats;
    },
    seedRooms(state) {
        return state.rooms;
    }
}